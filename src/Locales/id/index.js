import ValidationMessage from '@locales/id/Validation';
import CustomMessage from '@locales/id/Custom';

// merge all messages
const messages = {
  ...ValidationMessage,
  ...CustomMessage,
};

export default messages;
